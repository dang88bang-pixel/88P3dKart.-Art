fastapi==0.110.0
uvicorn[standard]==0.27.0
websockets==12.0
pyserial==3.5
numpy==1.24.3
scipy==1.10.1
python-multipart==0.0.6
pydantic==2.5.0
paho-mqtt==1.6.1

# Laufzeit-Abhängigkeit seit v4.5.0-Geo: Ichnaea-Provider und GTFS-RT-Abruf.
# Bewusst NICHT aufgenommen: protobuf / gtfs-realtime-bindings. Der Decoder in
# external/protobuf_lite.py liest die wenigen benötigten Felder direkt aus dem
# Wire-Format — das spart auf dem ARM-Image von CT45P rund 40 MB und einen
# C-Extension-Build.
httpx==0.27.0

# --- nur Entwicklung/Test ---
pytest==7.4.3
httpx==0.26.0
jsonschema==4.23.0
