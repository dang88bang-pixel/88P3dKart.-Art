<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- USB Host -->
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />

    <!-- Bluetooth LE -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- UWB (Android 12+) -->
    <uses-permission android:name="android.permission.UWB_RANGING" />

    <!-- Storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Foreground-Service für permanenten Sensorbetrieb (LiDAR+mmWave+UWB+BLE) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- ab Android 14 (API 34): typisierte FGS-Permits -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Netzwerk -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Wi-Fi RTT & RSSI-Scans (Triangulation, docs/TRIANGULATION.md) -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Phase 5 Härtung: Package-Visibility für den Honeywell-Scanner
         (Android 11+). Ohne <queries> liefert resolveActivity()/queryIntent-
         Activities() für die Honeywell-Dekodier-Dienste leer — der
         CT45pFilamentBridge-Honeywell-Receiver könnte den Scanner nicht
         anbinden. Abgedeckt: DataCollection-Intents (com.honeywell.decode)
         und AIDC (com.honeywell.aidc). -->
    <queries>
        <package android:name="com.honeywell.decode" />
        <package android:name="com.honeywell.aidc" />
        <intent>
            <action android:name="com.honeywell.decode.intent.ACTION" />
        </intent>
        <intent>
            <action android:name="com.honeywell.scan.decode.ACTION" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:label="3dxAgent"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThreeAgent">

        <activity
            android:name=".EnrollmentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false">
            <!-- USB-Device-Filter (RPLIDAR, FTDI mmWave) -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/usb_device_filter" />
        </activity>
    </application>
</manifest>
