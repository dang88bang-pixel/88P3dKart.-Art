<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        App-Theme. Parent ist absichtlich Theme.AppCompat.DayNight.NoActionBar,
        weil MainActivity von AppCompatActivity erbt. Theme.Material3 würde
        sonst beim Start eine IllegalStateException werfen ("You need to use a
        Theme.AppCompat theme"). Die Akzentfarben (primary/secondary/etc.)
        werden weiterhin aus colors.xml gezogen.
    -->
    <style name="Theme.ThreeAgent" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/secondary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/surface</item>
    </style>
</resources>
